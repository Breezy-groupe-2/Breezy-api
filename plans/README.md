# Implementation Plans

Generated by the improve skill on 2026-06-14. Execute in the order below unless dependencies say otherwise. Each executor must read the plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Establish real microservice boundaries for Breezy API | P1 | L | - | BLOCKED - umbrella plan was too broad; Step 2 broke auth-service feed tests after post extraction. Superseded by Plans 002-006. |
| 002 | Complete post-service extraction with feed compatibility | P1 | M | 001 blocked | DONE - verified on current HEAD; applied at commit `b524c82` and still passes focused tests, `npm test`, and `npm run lint`. |
| 003 | Align comment-service with public `/api/v1` routes | P1 | M | 002 | DONE - verified on current HEAD; applied at commit `da326ef` and still passes comment tests, focused Fx7/Fx8 acceptance, `docker compose config`, `npm test`, and `npm run lint`. |
| 004 | Extract follow ownership from auth-service | P1 | M | 002, 003 | DONE - verified on current HEAD; applied at commit `86a2d75` and still passes follow-service tests, focused Fx9 acceptance, `docker compose config`, `npm test`, and `npm run lint`. |
| 005 | Move feed ownership to feed-service through explicit contracts | P1 | M | 002, 004 | DONE - verified on current HEAD; applied at commit `86a2d75` and still passes feed-service tests, focused Fx5 acceptance, `docker compose config`, `npm test`, and `npm run lint`. |
| 006 | Add profile ownership and finalize architecture docs | P1 | M | 002, 003, 004, 005 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- Plan 001 is retained as the blocked record of the initial broad execution attempt. Do not retry it directly; execute Plans 002-006 instead.
- Plan 002 must run first because it recovers the stopped post extraction and keeps feed tests green with a temporary documented compatibility adapter.
- Plan 003 can run after Plan 002 because it depends on the composed public-contract test helper created during post extraction.
- Plan 004 runs after Plans 002 and 003 because it builds on the composed public-contract helper now mounting post and comment services; follow extraction must be available before feed-service uses follow data.
- Plan 005 depends on Plans 002 and 004 because feed composition needs post data and follow data with clear owners.
- Plan 006 comes last because profile responses aggregate user, post, like, and follow information, and it finalizes docs/OpenAPI after service ownership settles.
- Recommended follow-up plans after 006: per-service CI/test matrix, environment validation, dependency audit cleanup, Docker Compose gateway smoke tests, and service-to-service authentication if services become independently deployable.

## Findings considered and rejected

- "Route all public endpoints to auth-service": rejected because it conflicts with the confirmed real microservice architecture.
- "Keep post/feed implementations in auth-service for speed": rejected because it preserves the current architecture mistake and makes the gateway/service split misleading.
